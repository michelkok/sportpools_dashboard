export interface Rider {
    user: string;
    name: string;
    team: string;
    dnf: boolean;
    cost: number;
    gc_points: number;
    mountain_points: number;
    sprint_points: number;
    young_gc_points: number;
    points: number;
    isKluns?: boolean;
}
